version: '3.6'
services:
  web:
    container_name: recipes-searcher
    image: node:alpine
    command: sh -c "yarn run dev"
    working_dir: /usr/app
    volumes:
      - .:/usr/app/
    ports:
      - "8080:8080"
    expose:
      - "8080"
    environment:
      PORT: 8080
      HOST: 0.0.0.0